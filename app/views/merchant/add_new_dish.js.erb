$('.sort_paginate_ajax').html("<%= escape_javascript(render("merchant_dish_label_choose"))%>");

$(function() {
    $(".sort_paginate_ajax th a, .sort_paginate_ajax .pagination a").on("click", function(){
        update_present_choose();
        $.getScript(this.href);
        return false;
    });
});

set_labels_status();

